基本功能：
楼有 14 层，有 4 部自动电梯。每梯最多载客 12 人。从上班到下班不断有乘客使用电梯。电梯运行每上下一层需 5 秒，空闲时，在某一层最多停留 10 秒，否则返回 1 层待命。
乘客等待电梯时间不超过 T 时间。
（1）可以随机、文件及人工输入数据。
（2）实时显示电梯的工作运行状况。
（3）统计一天内每楼层电梯的使用人次数。
（4）可以查询和更新数据。

程序实现过程：

1.程序运行后先出现一个用户登录界面(先默认用户名和密码为admin，密码为123456，后续可以在程序中修改)，登陆成功后进入主界面，且登录界面有退出按钮，点击后退出程序
主界面布局(2-3)


2.楼层状态显示区
使用一个垂直的列表，列表内包含14个条目，这里条目要有足够宽和长，容纳四个电梯(电梯的条目要足够宽和长，容纳电梯的运行路径)，每个条目表示每层楼且有当前楼的标号--阿拉伯数字。
每层楼的状态可以用填充颜色的小球(放置在楼层右侧)来表示（例如，红色表示有等待请求，绿色表示空闲）当有新请求添加进来瞬间切换。
显示每层楼的状态，包括当前等待的乘客请求，请求用上述填充颜色的小球隐藏起来，
点击小球，小球附近展开成一个列表，细节包括{
列表的行数与当前楼层乘客希望到达的楼层的种类一致，从下往上
对应着期望到达楼层的从低到高，每行中包含""期望到达楼层"某层  "人数"多少
一、字体相关设置
字体大小：
展开列表中的文字统一采用系统默认的常规字体大小12像素；
字体颜色：
期望到达楼层的数字部分统一显示为黑色（RGB 值为 (0, 0, 0)），这样的颜色对比度高，易于识别。而对于人数部分，为了稍作区分，可以设置为深灰色（RGB 值为 (64, 64, 64)）。
二、对齐方式
水平对齐：
每行内容采用左对齐的方式。首先展示 “期望到达楼层” 的信息，紧接着是对应的 “人数” 信息，之间保持适当的间距（例如间隔 5 个像素），使整个列表看起来整齐有序，方便用户快速浏览各条乘客请求信息。
垂直对齐：
列表中的各行信息在垂直方向上保持顶部对齐，确保每一行的起始位置一致，使得整个列表呈现出规整的视觉效果，符合用户对于列表内容排列的常规预期。
三、列表宽度自适应
列表的宽度将根据内容长度自动进行自适应调整，以完整展示每行信息且尽量减少不必要的空白空间。具体实现方式如下：
计算每行文本内容中最长的 “期望到达楼层” 数字和对应的 “人数” 数字组合所占据的宽度（考虑文字本身的宽度以及间距等因素），然后在此基础上额外增加一定的边距（例如左右各 10 像素）作为列表的最小宽度。随着内容的增加或减少，列表宽度动态变化，确保不会出现文本被截断的情况，同时在内容较少时也不会显得过于空旷，始终保持良好的视觉呈现效果。
四、交互逻辑
点击展开：
当用户点击表示楼层状态的小球时，小球附近的列表将以动画形式展开显示，动画效果可以是淡入效果（从透明度 0 逐渐变为 1，动画时长约为 0.2 秒），使展开过程更加平滑自然，增强用户体验。
点击收起：
若用户在展开列表显示的状态下，点击除了该列表及相关操作按钮（如添加按钮）之外的界面其他任意地方，列表将自动以淡出效果（从透明度 1 逐渐变为 0，动画时长同样约为 0.2 秒）进行收起，恢复到隐藏状态，避免列表长时间占据界面空间影响其他功能区域的查看和操作，同时也符合常见的交互设计习惯，使用户操作更加便捷直观。），
}

且该列表顶部有一个添加按钮，可以以乘客求请求的的格式添加乘客，添加后，小球附近列表自动更新。
细节包括{
一、弹出输入框样式
大小与位置：
按下添加按钮后弹出的输入框大小适中，宽度设置为 200 像素，高度为 100 像素，在屏幕上的位置以小球展开列表为基准，水平居中且位于列表下方，与列表之间有 10 像素的间隔，使其看起来与列表紧密相关又不显得拥挤。
外观设计：
输入框具有浅灰色的边框（RGB 值为 (200, 200, 200)），边框宽度为 1 像素，内部背景色为白色（RGB 值为 (255, 255, 255)），给人简洁、清晰的视觉感受。在输入框的左上角显示提示文字，文字颜色为深灰色（RGB 值为 (80, 80, 80)），字体大小为 12 像素。
二、输入提示
初始提示：
当输入框弹出时，在输入框内左上角显示 “请输入期望到达楼层及人数（格式：楼层 人数）” 的提示文字，该提示文字以淡灰色显示（RGB 值为 (150, 150, 150)），当用户开始输入内容时，提示文字自动消失，避免对用户输入造成干扰。
格式示例提示：
在输入框的右下角，以更小的字体（10 像素）显示一个格式示例，如 “例如：5 3”，表示期望到达楼层为 5 楼，人数为 3 人，该示例文字颜色同样为深灰色，用于提醒用户正确的输入格式。
三、输入格式校验
实时校验：
在用户输入过程中，对输入内容进行实时校验。当用户输入完楼层和人数后，系统立即检查输入内容是否符合指定格式。
校验规则与提示：
如果用户输入的不是数字和空格组成的格式，例如输入了字母或其他特殊字符，输入框边框将立即变为红色（RGB 值为 (255, 0, 0)），并在输入框右侧显示一个红色的叉号图标（大小为 15 像素），同时在输入框下方弹出一个提示框，提示框背景色为白色，边框为红色，文字颜色为红色，显示 “输入格式错误，请输入数字和空格组成的格式”，提示框宽度为 200 像素，高度为 30 像素，位置紧贴输入框下方，显示时间为 2 秒后自动消失。
如果用户输入的楼层不在 1 到 14 之间，同样输入框边框变红并显示红色叉号图标，在输入框下方弹出提示框，提示文字为 “楼层范围错误，请输入 1 - 14 之间的楼层”，其他提示框样式和显示时间与格式错误提示相同。
如果用户输入的人数为 0 或负数，也按照上述错误提示方式进行提示，提示文字为 “人数错误，请输入大于 0 的整数”。
四、添加成功或失败反馈
添加成功：
当用户输入的内容符合格式且楼层和人数都在合理范围内时，系统将输入的乘客请求信息添加到对应的楼层乘客请求数据结构中，并更新小球展开列表的显示内容。同时，输入框边框变为绿色（RGB 值为 (0, 255, 0)），并在输入框右侧显示一个绿色的勾号图标（大小为 15 像素），在输入框上方弹出一个提示框，提示框背景色为白色，边框为绿色，文字颜色为绿色，显示 “添加成功”，提示框宽度为 100 像素，高度为 30 像素，位置位于输入框上方居中，显示时间为 1 秒后自动消失，然后输入框自动关闭，小球展开列表恢复到初始状态（如果之前有展开操作）。
添加失败（如系统已满等其他情况）：
若由于系统已满（例如所有电梯都已满载且无法再接受新的乘客请求）或其他非输入格式和范围错误导致添加失败，输入框边框变为黄色（RGB 值为 (255, 255, 0)），并在输入框右侧显示一个黄色的感叹号图标（大小为 15 像素），在输入框下方弹出提示框，提示框背景色为白色，边框为黄色，文字颜色为黄色，显示 “添加失败，系统已满，请稍后再试”，提示框样式和显示时间与错误提示相同。
}


3. 电梯状态显示区
各个楼层列表中添加4个垂直的条目，从左向右依次代表贯穿整栋楼的1-4号电梯路径,每个条目表示每部电梯出现的路径(电梯不在该楼层时绘制为电梯路径绘制为
白色，电梯在该楼层时该楼层的电梯路径绘制为灰色，离开时电梯路径再次恢复为白色)
细节如下{
一、过渡状态绘制样式
在电梯运行过程中，当处于楼层间过渡状态（即刚离开某楼层还未完全到达下一楼层时），采用渐变过渡的绘制样式，具体如下：
颜色渐变方向与范围：
以电梯离开的楼层对应的电梯路径条目为起点，到即将到达的下一楼层对应的电梯路径条目为终点，进行颜色的线性渐变。例如，电梯从 3 楼出发前往 4 楼，那么在这个过渡阶段，从 3 楼所在的电梯路径条目位置开始，颜色从灰色（表示离开时所在楼层）逐渐向白色（表示即将到达的下一楼层未到达时的状态）过渡，整个过渡过程覆盖电梯在这两层楼之间运行的路径范围。
渐变时间与帧数：
考虑到电梯每经过一层楼需耗费设定的时间（如默认 5 秒），将这个过渡阶段的渐变过程也设置在这 5 秒内完成。为了使渐变效果更加平滑自然，将这 5 秒划分为 10 帧（即每 0.5 秒更新一次绘制状态），每帧按照一定的比例改变颜色值，从灰色逐渐变淡直至接近白色，直至电梯到达下一楼层时完全变为白色。
二、颜色渐变具体算法
在进行颜色渐变时，采用基于 RGB 颜色空间的线性插值算法来计算每帧的颜色值，具体步骤如下：
确定起始和结束颜色的 RGB 值：
起始颜色为灰色，其 RGB 值设定为 (128, 128, 128)，表示电梯离开楼层时所在路径的颜色；结束颜色为白色，RGB 值为 (255, 255, 255)，代表即将到达楼层但未到达时路径的预期颜色。
计算每帧的颜色差值：
对于每个颜色通道（R、G、B），分别计算在 10 帧内每帧颜色需要改变的差值。例如，对于红色通道（R），差值计算公式为：(结束颜色的 R 值 - 起始颜色的 R 值) / 总帧数，即 (255 - 128) / 10 = 12.7（这里可根据实际情况进行取整或保留小数处理，如取整为 13）。
更新每帧颜色值：
从第一帧开始，按照计算出的差值逐步更新每个颜色通道的值，以第一帧为例，红色通道的值更新为起始颜色的 R 值 + 每帧颜色差值（即 128 + 13 = 141），同理更新绿色通道和蓝色通道的值，得到第一帧的过渡颜色 RGB 值，后续每帧以此类推，直到第 10 帧颜色接近或达到白色的 RGB 值 (255, 255, 255)。
}

电梯条目中显示每部电梯的当前位置(楼层数)、运行方向(可以插入三个图标，向上时箭头、向下箭头、圆圈，内部填充为白色，当处于某状态时，填充为红色)、载客人数。
运行方向图标显示细节细节包括{
一、图标整体大小与位置摆放
图标大小：
图标整体尺寸设置为边长 20 像素的正方形，这样的大小既能够在电梯条目显示区域内清晰可辨，又不会占据过多空间而影响其他信息（如楼层数、载客人数等）的展示。
位置摆放：
图标位于电梯条目的右上角，距离电梯条目上边框和右边框均为 5 像素的位置。这个位置处于用户视觉焦点区域，方便用户快速查看电梯的运行方向状态，同时与其他信息布局协调，使整个电梯条目显示更加规整有序。
二、图标切换动画效果
动画类型与时长：
当电梯运行方向发生切换时，采用旋转渐变动画效果来过渡图标显示。动画时长设定为 0.3 秒，这个时长能够在提供流畅视觉体验的同时，不过分延迟信息的更新展示，使用户能够及时感知到电梯运行方向的变化。
动画过程描述：
以向上箭头图标切换到向下箭头图标为例，动画开始时，向上箭头图标以自身中心为轴，顺时针旋转 90 度，在旋转过程中，图标内部填充颜色从白色逐渐过渡到透明，旋转到 45 度时，透明程度达到 50%；当旋转到 90 度时，透明程度为 100%，此时向上箭头图标完全不可见。
紧接着，向下箭头图标从透明状态开始，以同样的中心轴逆时针旋转 -90 度（即从垂直向下方向旋转到水平向右方向），在这个过程中，图标内部填充颜色从透明逐渐过渡到红色，旋转到 -45 度时，填充颜色的红色程度达到 50%；当旋转到 0 度（即垂直向下方向）时，填充颜色完全变为红色，完成向下箭头图标的切换显示。
在整个动画过程中，图标大小保持不变，并且过渡平滑自然，通过计算不同阶段的旋转角度、颜色透明度和填充颜色比例等参数，利用图形渲染技术实现流畅的动画效果，避免出现闪烁、卡顿等不良视觉现象。
}
载客人数显示更新频率细节包括{
更新频率
实时更新：载客人数显示应该是实时更新的。当电梯门打开，乘客进入或离开电梯时，载客人数的显示会立即发生变化。这样可以让用户最直观地了解电梯的载客情况，以便更好地理解电梯的运行状态和分配乘客请求。例如，当电梯停靠在某一层，有 5 名乘客进入电梯后，载客人数会马上在电梯状态显示区的对应电梯条目上增加 5。
数字格式
整数格式：载客人数显示为整数。因为电梯载客人数是按照整个人数来计算的，不存在小数的情况。数字的字体大小可以设置为 14 像素，颜色为黑色（RGB 值为 (0,0,0)），以确保清晰可读。并且数字在电梯条目的显示位置为中部偏左，与运行方向图标和楼层数等信息保持合理的间距，例如距离左边框 10 像素，垂直方向与运行方向图标和楼层数居中对齐，这样整个电梯状态显示区的布局会更加美观、整齐。
}

电梯运行时每上下一层需 5 秒(可在设置区修改)，即经过5s后当前电梯条目恢复为白色路径状态，电梯前往下一楼层条目，下一路径条目变成电梯状态。
这里电梯的运行逻辑应该便于扩展和修改：

电梯运行逻辑如下：
{
一、初始化阶段
电梯状态初始化
四个电梯分别初始化为位于一楼，电梯门关闭，运行方向设为无（静止状态），内部目标无（即当前内部无乘客请求），已运行时间、空闲停留时间均清零。
每个电梯经过每层楼的时间默认5s（可后续在设置区修改），开关门时间默认1s（可后续在设置区修改），并设定电梯最大容量为默认的 20 人（可后续在设置区修改）,
空闲停留时间默认20s(可后续在设置区修改)，用于判断电梯空闲时是否返回一楼或继续等待新请求。
乘客请求记录添加：
可以通过上述的楼层状态显示区手动添加乘客请求，
也可以通过读取txt文件添加乘客请求，文件格式为"期望到达楼层" 某层  "人数"，
例如“1 2 3 1”表示有3个乘客希望到达2楼，且电梯1号分配给这3个乘客。
期间每10s(可在设置区修改)随机生成几个(1-4个)乘客请求(乘客当前楼层随机，乘客请求的期望到达楼层随机，人数随机)，乘客请求的期望到达楼层随机，人数随机。
创建一个数据结构（如列表或队列等）用于记录各个楼层的乘客请求，格式为 “期望到达楼层” 某层 “人数” 多少，初始时该数据结构为空，后续实时接收并更新乘客请求信息。
这里乘客请求分配的电梯是随机分配的，后续可以修改为根据电梯的当前状态进行分配。
二、乘客请求处理
接收新请求
实时监测并接收新的乘客请求信息，按照 “期望到达楼层” 某层 “人数” 多少 “电梯分配” 几号的格式记录到乘客请求数据结构中。
请求分配逻辑
对于新接收到的每个乘客请求，遍历四个电梯的当前状态：
若有电梯处于空闲状态（即无运行任务且当前无乘客），则将该请求分配给此空闲电梯，电梯根据当前楼层与请求的期望到达楼层关系确定运行方向并启动运行。
若没有空闲电梯，则查看各个正在运行的电梯的运行方向和任务情况：
优先分配给运行方向与请求期望到达楼层方向相符（如电梯正在向上运行，请求也是去往更高楼层；或电梯正在向下运行，请求是去往更低楼层）且后续停靠楼层顺路的电梯。例如，电梯 1 从 3 楼向上运行，已经要停靠 5 楼和 7 楼，此时来了一个去往 6 楼的请求，就可分配给电梯 1。
如果没有符合上述顺路条件的电梯，则选择当前任务量（已登记的停靠次数和乘客数量综合考量）相对较少的电梯进行分配，该电梯会根据新分配的请求以及已有的任务重新规划运行顺序和方向。
三、电梯运行阶段
移动过程
每个电梯按照分配的任务及确定的运行方向开始运行，每经过一层楼，耗费设定的经过每层楼的时间（如 2 秒，可在设置区修改），同时持续判断是否到达需要停靠的楼层。
停靠判断
到达某楼层时，检查该楼层是否存在针对本电梯的外部呼叫（即此楼层有乘客期望乘坐本电梯且分配给了该电梯）或者内部有乘客要到达该楼层（此前在电梯内按下了对应楼层按钮）：
若是，则电梯开始执行开门操作，耗费设定的开关门时间（如 1 秒，可在设置区修改）。开门后，若当前楼层有分配给本电梯且等待的乘客，且电梯内未达到最大容量，当前楼层乘客会全部涌入电梯直至达到最大容量，多余乘客继续等待；同时，电梯内要在该楼层下的乘客出电梯。更新电梯内的目标楼层记录以及该楼层的外部呼叫状态（已响应的清除）。
若否，则电梯不停靠，继续按照原运行方向运行前往下一楼层。
四、电梯空闲处理
空闲监测
每个电梯在完成一次运送乘客任务（即电梯内乘客全部送出后），进入空闲状态，开始计时空闲停留时间。
返回一楼判断
若电梯当前不在一楼，且空闲停留时间达到了设定的最大空闲停留时间（如 20 秒，可在设置区修改），电梯将自动改变运行方向（若需要），返回一楼，在返回过程中遵循上述的运行和停靠逻辑。
若在空闲停留时间未达到最大空闲停留时间时，检测到有新的乘客请求分配给了该电梯，则电梯立即根据新请求确定运行方向并前往相应楼层，开始新的运送任务。
五、运行起始和停止
点击控制区的开始按钮后，整个系统持续运行，不断接收新的乘客请求，四个电梯依据上述逻辑持续完成乘客运送任务、处理空闲状态等，以模拟电梯在 14 层建筑中的实际运行情况。
停止按钮后，整个系统停止运行，且电梯立刻回到一楼，并清空所有数据（当前乘客请求、电梯状态、电梯运行时间、电梯空闲停留时间），恢复到初始状态，图标也销毁。
}



4.设置区
在主界面上提供一个设置菜单栏，菜单栏中包含一个"设置"选项，点击后弹出设置窗口，
设置窗口中包含
一个"电梯最大搭乘人数"(对四个电梯通用)的输入框，设置窗口中"电梯最大搭乘人数"的输入框用于输入当前四个电梯的最大载客量，
一个"上下楼时间：   (s/floor)"的输入框，"上下楼时间：   (s/floor)"的输入框用于输入电梯经过每楼层的时间，
一个"空闲停靠时间"的输入框，"空闲停靠时间"的用于输入电梯空闲时在某一层最多停留的时间，
一个“一天时间： s/day”(至少50s,以10为单位递增，上限200s), "一天时间： s/day"的用于输入一天的时间，
一个"随机乘客请求： s/request"的输入框，"随机乘客请求"的用于输入随机乘客请求的频率，即几秒生成几个乘客请求，
一个"退出"的按钮，"保存"的按钮用于退出程序。

输入框细节包括{
一、“电梯最大搭乘人数” 输入框
范围限制：
允许输入的最小值为 1，最大值为 50（可根据实际模拟需求设定合理上限）。输入框只接受正整数输入，不允许输入负数、小数、字母或其他特殊字符。
提示信息：
当输入框获得焦点时，在输入框内显示淡灰色的提示文字 “请输入 1 - 50 之间的正整数作为电梯最大搭乘人数”。一旦用户开始输入内容，提示文字自动消失。
如果用户输入了负数，例如 -5，输入框边框立即变为红色，在输入框右侧显示一个红色的叉号图标，并在输入框下方弹出一个提示框，提示框背景色为白色，边框为红色，文字颜色为红色，显示 “电梯最大搭乘人数不能为负数，请重新输入”，提示框宽度为 250 像素，高度为 30 像素，位置紧贴输入框下方，显示时间为 2 秒后自动消失。
若输入非整数（如 10.5）或特殊字符（如 “abc”），同样输入框边框变红，显示红色叉号图标和提示框，提示文字为 “输入格式错误，请输入正整数”。当输入值大于 50 时，提示框文字为 “输入值过大，最大搭乘人数应在 1 - 50 之间”。
二、“上下楼时间” 输入框
范围限制：
允许输入的最小值为 1 秒，最大值为 10 秒（可根据实际情况调整范围）。只接受正整数输入，不允许输入负数、小数、字母或其他特殊字符。
提示信息：
输入框获得焦点时，显示淡灰色提示文字 “请输入 1 - 10 之间的正整数作为电梯上下楼时间（单位：秒）”，输入时提示文字消失。
若输入负数（如 -2），输入框边框变红，显示红色叉号图标和提示框，提示文字为 “上下楼时间不能为负数，请重新输入”。输入非整数或特殊字符时，提示框文字为 “输入格式错误，请输入正整数”。当输入值大于 10 时，提示框文字为 “输入值过大，上下楼时间应在 1 - 10 秒之间”。
三、“空闲停靠时间” 输入框
范围限制：
最小值为 5 秒，最大值为 30 秒。只接受正整数输入。
提示信息：
焦点在输入框时，显示 “请输入 5 - 30 之间的正整数作为电梯空闲停靠时间（单位：秒）”。输入负数时，如 -3，输入框边框变红，显示红色叉号图标和提示框 “空闲停靠时间不能为负数，请重新输入”。非整数或特殊字符输入时，提示框文字为 “输入格式错误，请输入正整数”。输入值大于 30 时，提示框文字为 “输入值过大，空闲停靠时间应在 5 - 30 秒之间”。
四、“一天时间” 输入框
范围限制：
按照要求，最小值为 50 秒，最大值为 200 秒，且以 10 为单位递增，即只能输入 50、60、70……200 这些值中的一个。只接受正整数输入。
提示信息：
输入框获得焦点时，提示 “请输入 50 - 200 之间以 10 为单位递增的正整数作为一天时间（单位：秒）”。若输入不在此范围内的值，如 45，输入框边框变红，显示红色叉号图标和提示框 “一天时间输入错误，请输入 50 - 200 之间以 10 为单位递增的正整数”。输入非整数或特殊字符时，提示框文字为 “输入格式错误，请输入正整数”。
五、“随机乘客请求” 输入框
范围限制：
最小值为 1 秒，最大值为 60 秒。只接受正整数输入。
提示信息：
焦点在输入框时，显示 “请输入 1 - 60 之间的正整数作为随机乘客请求生成时间间隔（单位：秒）”。输入负数（如 -1）时，输入框边框变红，显示红色叉号图标和提示框 “随机乘客请求时间间隔不能为负数，请重新输入”。非整数或特殊字符输入时，提示框文字为 “输入格式错误，请输入正整数”。输入值大于 60 时，提示框文字为 “输入值过大，随机乘客请求时间间隔应在 1 - 60 秒之间”。
}



5.控制区
在主界面最左侧提供两个按钮，一个"开始"，一个"重置"。
点击"开始"后，整个电梯模拟系统开始按照上述运行逻辑运行，并开始计时(统计一天时间)，期间不可暂停。
当经过当天时间的6/24时，强制随机在一楼暴增一次乘客请求(与上述每隔固定时间随机生成乘客请求不同，这里是在固定时间点强制生成乘客请求，不当做上述随机生成的少量乘客请求)(随机暴增10-15个乘客请求，乘客当前楼层固定为1楼，乘客请求的期望到达楼层随机，乘客人数随机)，
开始按钮上方空白部分显示个“上班”的字符串，2秒后消失。
当经过当天时间的11/24时，非1楼乘客人数随机增加一次乘客请求（与上述每隔固定时间随机生成乘客请求不同，这里是在固定时间点强制生成乘客请求，不当做上述随机生成的少量乘客请求）（随机暴增10-15个乘客请求，乘客当前楼层随机，乘客请求的期望到达楼层固定为1楼，乘客人数随机），
开始按钮上方空白部分显示个“下班”的字符串，2秒后消失。
当经过当天时间的14/24时，随机在一楼暴增一次乘客请求（与上述每隔固定时间随机生成乘客请求不同，这里是在固定时间点强制生成乘客请求，不当做上述随机生成的少量乘客请求）(随机暴增10-15个乘客请求，乘客当前楼层固定为1楼，乘客请求的期望到达楼层随机，乘客人数随机)。
开始按钮上方空白部分显示个“上班”的字符串，2秒后消失。
当经过当天时间的16/24时，非1楼乘客人数随机增加一次乘客请求（与上述每隔固定时间随机生成乘客请求不同，这里是在固定时间点强制生成乘客请求，不当做上述随机生成的少量乘客请求）（随机暴增10-15个乘客请求，乘客当前楼层随机，乘客请求的期望到达楼层固定为1楼，乘客人数随机），
开始按钮上方空白部分显示个“下班”的字符串，2秒后消失。

当点击“开始”且电梯运行经过一天时间后，弹出两张表，
其中一张为柱状图，横轴上14个柱体表示1-14楼，纵轴中14 个柱体的高度表示1天中该楼乘客使用电梯的次数(最大显示999次)，柱顶标出电梯使用次数。
另一张为折线图，横轴分为24个柱体，从左到右每个柱体表示1小时的时间段，比如0-1小时，1-2小时，2-3小时，...，23-24小时，纵轴表示该时间段内4个电梯中运行的电梯个数。

点击"重置"后，四个电梯停止运行，电梯立刻回到一楼，并清空所有数据，恢复到初始状态，图标也销毁。


在主界面布局区的合适位置留出LogOut按钮，点击后返回登录界面


UI界面设计使用Qt5，使用信号与槽机制，使用多线程支持 